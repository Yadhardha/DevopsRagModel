- name: Devops Rag Model
  on:
    push:
      branches:
        - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: Run sample command
        run: echo "pipeline running"
      - name: sample error
        run: exit 1